apiVersion: v1
kind: ConfigMap
metadata:
  name: cilium-lb-ip-pools-placeholder
  namespace: argocd
data:
  note: Keep your existing Cilium LoadBalancer IP pool resources here if you want them tracked in Git, but this component is not Argo-managed in the current setup.
